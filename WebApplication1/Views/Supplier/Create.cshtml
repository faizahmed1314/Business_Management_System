@model List<WebApplication1.Models.Supplier>
@{
    ViewBag.Title = "Create";
}

<h2>Supplier Registration</h2>

<div class="row">
    <div class="col-sm-9 col-md-7 col-lg-5 mx-auto">
        <div class="card card-save my-5">
            <div class="card-body">
                <h5 class="card-title text-center"><strong>Supplier Entity</strong></h5>
                <form method="POST" class="form-save" enctype="multipart/form-data">
                    @Html.AntiForgeryToken()

                    <div class="form-label-group">
                        <label for="inputCode"> Code</label>
                        <input type="text" id="Code" name="Code" class="form-control" placeholder="Supplier Code" required autofocus>

                    </div>
                    <div class="form-label-group">
                        <label for="inputName"> Name</label>
                        <input type="text" id="Name" name="Name" class="form-control" placeholder="Supplier Name" required autofocus>

                    </div>
                    <div class="form-label-group">
                        <label for="inputAddress"> Address</label>
                        <input type="text" id="Address" name="Address" class="form-control" placeholder="Supplier Address" required autofocus>

                    </div>
                    <div class="form-label-group">
                        <label for="inputEmail"> Email</label>
                        <input type="text" id="Email" name="Email" class="form-control" placeholder="Supplier Email" required autofocus>

                    </div>
                    <div class="form-label-group">
                        <label for="inputNumber">Contact Number</label>
                        <input type="text" id="ContactNumber" name="ContactNumber" class="form-control" placeholder="Supplier Contact Number" required autofocus>

                    </div>
                    <div class="form-label-group">
                        <label for="inputContactPerson">Contact Person</label>
                        <input type="text" id="ContactPerson" name="ContactPerson" class="form-control" placeholder="Contact Person" required autofocus>

                    </div>
                    <br />
                    <div>
                        <label class="col-sm-2 control-label">Image</label>
                        <div class="col-sm-10">
                            <img src="" class="img-responsive img-thumbnail pch-img" id="cntimage" alt="select image">

                            <input type="file" name="UploadFile" required="required" onchange="readURL(this);">
                        </div>
                    </div>
                    <br />
                    <br />
                    <button class="btn btn-lg btn-info btn-block text-uppercase" type="submit">Save</button>
                </form>
            </div>

        </div>


    </div>
</div>
<br />
<div>
    @Html.ActionLink("Back To Index", "Index")
</div>
@if (Model != null && Model.Count > 0)
{
    <div>
        @{var sl = 0;}
        <table class="table table-bordered table-striped table-responsive">
            <thead>
                <tr>
                    <th>Sl</th>
                    <th>Code</th>
                    <th>Name</th>
                    <th>Address</th>
                    <th>Email</th>
                    <th>Contact Number</th>
                    <th>Loyalty Point</th>
                    <th>Image</th>
                    <th>Action</th>

                </tr>
            </thead>
            <tbody>
                @foreach (var supplier in Model)
                {
                    <tr>
                        <td>@(++sl)</td>
                        <td>@supplier.Code</td>
                        <td>@supplier.Name</td>
                        <td>@supplier.Address</td>
                        <td>@supplier.Email</td>
                        <td>@supplier.ContactNumber</td>
                        <td>@supplier.ContactPerson</td>

                        <td>
                            @{
                    var file = Convert.ToBase64String(supplier.File);
                    var image = string.Format("data:image/gif; base64," + file + "");
                            }
                            <img class="img-responsive img-thumbnail" src="@image" style=" width: 100px; height: 100px;" />
                        </td>


                        <td>
                            @Html.ActionLink("Edit", "Edit", "Customer", new { id = supplier.Id }, new { @target = "_self" })
                            @Html.ActionLink("Delete", "Delete", "Customer", new { id = supplier.Id }, new { @target = "_self" })

                        </td>
                    </tr>
                }

            </tbody>
        </table>
    </div>
}

<script type="text/javascript">
    function readURL(input) {
        if (input.files && input.files[0]) {
            var reader = new FileReader();

            reader.onload = function (e) {
                $('#cntimage')
                    .attr('src', e.target.result);
            };

            reader.readAsDataURL(input.files[0]);
        }
    }
</script>

